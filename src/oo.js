"use strict";
// Orientação a objetos com TypeScript
class Pessoa {
    constuctor(nome, renda) {
        this.nome = nome;
        this.renda = this.renda;
    }
    dizOla() {
        return `Olá, ${this.nome}`;
    }
}
